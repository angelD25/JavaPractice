package InterFaces;

public class Bank_ABC implements Bank {
 public int getInterestRate() {
	 return 5;
 }
}

//inheriting to classes from classes --> extends
// inheriting in classes from interface --> implements
//inheriting to interface from interface --> extends
